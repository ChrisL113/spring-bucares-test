pipeline {
    agent any 
    stages{
        stages("test"){
            steps{
                echo "this is where sonarqube should go"
            }
        }
        stages("deploy"){
            steps {

            }
        }
    }
}